{
  "name": "ucpt-kids-root",
  "version": "1.0.0",
  "description": "Orquestador para UCPT Kids en Koyeb",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "pg": "^8.11.5"
  }
}